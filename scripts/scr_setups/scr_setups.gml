function scr_setups()
{
	// configure the decomp's optional things here
	global.smoothcam = true; // toggles the smooth camera
	global.tv = false; // toggles the TV hud over the head hud
	global.funny = "?"; // set this to true
	// fin
}